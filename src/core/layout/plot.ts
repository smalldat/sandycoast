/**
 * Fraction of the vertical layout box a chart's data may occupy, leaving
 * headroom on top so the tallest value doesn't touch the plot edge. Shared by
 * every cartesian visual so bars, lines and their axis ticks agree on where
 * "full scale" is.
 */
export const PLOT_HEIGHT = 0.92;
